# ============================================
# SwagSuite Environment Configuration
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit .env file to git!

# ============================================
# REQUIRED - Database Configuration
# ============================================
# PostgreSQL connection string
# Format: postgresql://username:password@host:5432/database
# For Neon: postgresql://username:password@endpoint.neon.tech/database?sslmode=require
# For Cloud SQL: postgresql://username:password@/database?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME
DATABASE_URL=postgresql://username:password@localhost:5432/swagsuite_db

# ============================================
# REQUIRED - Server Configuration
# ============================================
# Port (Cloud Run akan override ke 8080)
PORT=5000
# Environment: development, production
NODE_ENV=development

# ============================================
# REQUIRED - Security Secrets
# ============================================
# JWT Secret untuk token generation (minimal 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-here-min-32-chars

# Session Secret untuk session encryption (minimal 32 characters)
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-session-secret-here-min-32-chars

# ============================================
# REQUIRED - Cloudinary (File Uploads)
# ============================================
# Get these from https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ============================================
# OPTIONAL - Email Configuration
# ============================================
# For SendGrid: smtp.sendgrid.net, port 587, user: apikey
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password

# ============================================
# OPTIONAL - External Integrations
# ============================================
# S&S Activewear Integration
SS_ACTIVEWEAR_ACCOUNT=your-account-number
SS_ACTIVEWEAR_API_KEY=your-api-key

# Anthropic AI (untuk AI features)
ANTHROPIC_API_KEY=your-anthropic-api-key

# ============================================
# OPTIONAL - Application Settings
# ============================================
# Frontend URL untuk CORS
CLIENT_URL=http://localhost:5173

# Default role untuk user baru: user, admin, manager
DEFAULT_USER_ROLE=user

# ============================================
# Cloud Run Specific (tidak perlu di-set untuk local dev)
# ============================================
# Google Cloud Project ID (auto-set oleh Cloud Run)
# GOOGLE_CLOUD_PROJECT=your-project-id

# Google Cloud Region (auto-set oleh Cloud Run)
# GOOGLE_CLOUD_REGION=asia-southeast2