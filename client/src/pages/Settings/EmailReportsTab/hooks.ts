// useEmailReportsTab hook
// This component is currently static (no state, queries, mutations, or forms).
// The hook is provided as a scaffold for future interactivity.

export function useEmailReportsTab() {
  return {};
}
